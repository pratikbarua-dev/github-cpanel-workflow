# Database Configuration
# Options: 'sqlite' (Development) or 'mysql' (Production)
DB_TYPE=sqlite

# MySQL Configuration (Only used if DB_TYPE=mysql)
DB_HOST=localhost
DB_USER=root
DB_PASS=password
DB_NAME=mail_client
